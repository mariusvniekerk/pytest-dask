# Config file for automatic testing at travis-ci.org

sudo: false
language: python

matrix:
    include:
      - python: 2.7
      - python: 3.5
      - python: 3.6
      - python: pypy
      
install:
  - pip install pytest dask

script:
  - pip install .
  - pytest -vrsx
